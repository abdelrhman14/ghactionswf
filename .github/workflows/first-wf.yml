name : First GitHub WF

on: push

jobs: 
    first_job:
        runs-on: ubuntu-latest
        steps:

          - name: Checkout Repo
            uses: actions/checkout@v7

          - name: Welcom Message
            run: echo "My First Workflow"

          - name: List all files
            run: | 
                ls
                ls -ltra
          - name: Generate ASCII Network
            run: cowsay -f dragon "Run for cover, Hi abdo" >> dragon.txt 

          - name: Read File
            run: cat README.md